VERSION=${PACKAGE_VERSION}.${PKG_RELEASE}
VERSION_DATE="${GIT_COMMIT_DATE}"
PKG_TARGET=${PKG_TARGET}
PKG_TARGET_ARCH=${PKG_TARGET_ARCH}
PKG_TARGET_VERSION=${PKG_TARGET_VERSION}
PKG_TARGET_NVR=${PKG_TARGET_NVR}
PKG_UPLOAD_NAME=${PKG_UPLOAD_NAME}
PKG_TAR_FILE=${PKG_TAR_FILE}
PKG_XML_FILE=${PKG_XML_FILE}
